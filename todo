+ execution
    ./builtin
        each function with it's filename
    ./evaluate
        command
        heredoc
        expanding
        pipe
        and / or
        parenteses
        nodes
        open_file

+ parsing:
    tokenize
    read_input

+ utils
    new
    list.c
    signals

+ make sure to set all pointers to NULL